{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_cuestionario_new' %}

{% block main %}
    <h1>{{ 'title.cuestionario_new'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.summary) }}
        {{ form_row(form.content) }}
        {{ form_row(form.publishedAt) }}
{#        {{ form_row(form.tags) }}#}

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'label.create_cuestionario'|trans }}
        </button>
        {{ form_widget(form.saveAndCreateNew, {label: 'label.save_and_create_new', attr: {class: 'btn btn-primary'}}) }}
        <a href="{{ path('admin_cuestionario_index') }}" class="btn btn-link">
            <i class="far fa-list-alt" aria-hidden="true"></i> {{ 'action.back_to_list'|trans }}
        </a>
    {{ form_end(form) }}
{% endblock %}

{% block sidebar %}
    {{ parent() }}

{#    {{ show_source_code(_self) }}#}
{% endblock %}
