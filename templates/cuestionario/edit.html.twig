{% extends 'base.html.twig' %}

{% block title %}Edit Cuestionario{% endblock %}

{% block body %}
    <h1>Edit Cuestionario</h1>

    {{ include('cuestionario/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('cuestionario_index') }}">back to list</a>

    {{ include('cuestionario/_delete_form.html.twig') }}
{% endblock %}
