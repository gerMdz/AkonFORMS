{% extends 'base.html.twig' %}

{% block title %}Edit Formulario{% endblock %}

{% block body %}
    <h1>Edit Formulario</h1>

    {{ include('formulario/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('formulario_index') }}">back to list</a>

    {{ include('formulario/_delete_form.html.twig') }}
{% endblock %}
