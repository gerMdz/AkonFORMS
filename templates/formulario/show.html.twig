{% extends 'base.html.twig' %}

{% block title %}Formulario{% endblock %}

{% block body %}
    <h1>Formulario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formulario.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ formulario.createdAt ? formulario.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ formulario.updatedAt ? formulario.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formulario_index') }}">back to list</a>

    <a href="{{ path('formulario_edit', {'id': formulario.id}) }}">edit</a>

    {{ include('formulario/_delete_form.html.twig') }}
{% endblock %}
