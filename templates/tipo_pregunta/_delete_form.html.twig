<form method="post" action="{{ path('tipo_pregunta_delete', {'id': tipo_preguntum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_preguntum.id) }}">
    <button class="btn">Delete</button>
</form>
