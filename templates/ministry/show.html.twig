{% extends 'base.html.twig' %}

{% block title %}Ministry{% endblock %}

{% block body %}
    <h1>Ministry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ministry.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ministry.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ ministry.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Hashkey</th>
                <td>{{ ministry.hashkey }}</td>
            </tr>
            <tr>
                <th>Identificador</th>
                <td>{{ ministry.identificador }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ministry_index') }}">back to list</a>

    <a href="{{ path('ministry_edit', {'id': ministry.id}) }}">edit</a>

    {{ include('ministry/_delete_form.html.twig') }}
{% endblock %}
