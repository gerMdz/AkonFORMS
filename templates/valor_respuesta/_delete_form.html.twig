<form method="post" action="{{ path('valor_respuesta_delete', {'id': valor_respuestum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ valor_respuestum.id) }}">
    <button class="btn">Delete</button>
</form>
